import React from 'react'
import "./Result.scss";

export const Bullet = ({extra}) => {

    let className = "Bullet" + " " + extra

    return (
        <div className = {className}>
            
        </div>
    )
}
